[core]
	editor = vim
[push]
	default = simple
[merge]
	tool = meld
[credential]
	helper = /usr/lib/git-core/git-credential-gnome-keyring
[diff]
	tool = meld
[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
	unstage = reset HEAD --
	last = log -1 HEAD
